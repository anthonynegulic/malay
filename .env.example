# Copy to .env and fill in. Never committed (see .gitignore).
ANTHROPIC_API_KEY=sk-ant-...
# Optional override; defaults to claude-sonnet-4-6
CLAUDE_MODEL=claude-sonnet-4-6
# Optional server port; defaults to 8787
PORT=8787
